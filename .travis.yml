language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
install:
  - yarn install
  - yarn snyk:auth
script:
  - yarn snyk:test
  - yarn coverage
  - yarn build
  - yarn coveralls
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master
